import React from "react";

// this stateless function will render the 
// streaming twitter data in the app's react hook
function TwitterFeed() {
    return (
        //Place Material UI JSX here
    );
}

export default TwitterFeed;

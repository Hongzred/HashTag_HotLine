diff a/src/components/Map/Map.js b/src/components/Map/Map.js	(rejected hunks)
@@ -32,10 +32,7 @@ class Map extends React.Component {
-			const markers = issues.map(({ location: { longitude, latitude },
-				post: description, id: _id }) => (
-				<Marker
-					key={_id}
-					latitude={latitude}
-					longitude={longitude}
-					offsetLeft={-10}
-					offsetTop={-25}
-				>
-					<Pin
+			const markers = issues.map(
+				({
+					location: { longitude, latitude },
+					post: description,
+					id: _id,
+				}) => (
+					<Marker
@@ -43,8 +40,16 @@ class Map extends React.Component {
-						pinClickHandler={this.pinClickHandler.bind(this, {
-							latitude,
-							longitude,
-							description,
-						})}
-					/>
-				</Marker>
-			))
+						latitude={latitude}
+						longitude={longitude}
+						offsetLeft={-10}
+						offsetTop={-25}
+					>
+						<Pin
+							key={_id}
+							pinClickHandler={this.pinClickHandler.bind(this, {
+								latitude,
+								longitude,
+								description,
+							})}
+						/>
+					</Marker>
+				),
+			)
